export class Movie {
  constructor(
    plot: string,
    title: string,
    actors: string,
    awards: string,
    country: string,
    poster: string,
    language: string,
    imdbRating: string,
  ) {
    this.plot = plot;
    this.actors = actors;
    this.title = title;
    this.awards = awards;
    this.country = country;
    this.poster = poster;
    this.language = language;
    this.country = country;
    this.poster = poster;
    this.imdbRating = imdbRating;
  }
}
